# Identificação do projeto no Docker (prefixo dos containers)
COMPOSE_PROJECT_NAME=implantation-system

# Banco de dados
DB_HOST=localhost
DB_PORT=5432
DB_NAME=implantation_db
DB_USER=implantation_user
DB_PASSWORD=change_me

# Aplicação
APP_PORT=8080
APP_PROFILE=dev

# JWT
JWT_SECRET=change_me_use_at_least_256_bits_secret_key_here
JWT_EXPIRATION_MS=86400000
